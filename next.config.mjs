/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    experimental: {
        appDir: true, // Optional: if using the new app directory structure
    },
};

export default nextConfig;
